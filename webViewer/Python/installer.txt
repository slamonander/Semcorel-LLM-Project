pip install Flask
pip install flask_cors
pip install requests
pip install langchain PyPDF2 sentence-transformers faiss-cpu transformers accelerate
pip install -U langchain-community
pip install pymupdf
pip install PyPDF2